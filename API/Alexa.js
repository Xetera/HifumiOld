/* Imports the Dialogflow library
const Alexa = require('alexa-voice-service');
const config = require('../../config0');
const options = {
    debug: true,
    clientId: config.alexa.appID,
    clientSecret: config.alexa.ClientSecret
};


const avs = new Alexa(options); */